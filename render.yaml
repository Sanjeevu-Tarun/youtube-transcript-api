services:
  - type: web
    name: youtube-transcript-api
    runtime: docker
    plan: free          # change to "starter" ($7/mo) for always-on
    envVars:
      - key: SECRET_TOKEN
        sync: false     # set this in Render dashboard — leave blank for open access
